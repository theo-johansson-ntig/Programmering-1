length = float(input("Vad är rektangelns längd? "))
width = float(input("Vad är rektangelns bredd? "))

circumference = (2*length) + (2*width)
area = length * width

print(f"\nRektangelns omkrets är {circumference}")
print(f"Rektangelns area är {area}")